import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DreamLoop app smoke test', (WidgetTester tester) async {
    // Basic smoke test placeholder
    // Full widget tests require Firebase mocking
    expect(1 + 1, equals(2));
  });
}
